# DESIGN — 승인 시안 보존 규칙

## 기준
사용자가 ‘생각한 방향대로 그려졌고 화면이 마음에 든다’고 승인한 실제 파일은 `references/jobprep_dark_prototype.html`이다.
- 원본 SHA-256: `f1e964ee2428e8e5da3505b313661639495a746dd9015767200b4e6bb13f3619`
- 보조 이미지: `references/home-preview.png`.
- 화면·색상·정보 위계는 이 파일을 기준으로 한다. 이전 라이트모드/청록색 텍스트 제안보다 실제 승인 파일이 우선한다.
- 비즈니스 동작은 PRODUCT의 마지막 결정이 우선한다. 특히 Slack은 D-1·당일과 갱신 요약만 제공한다.
- 참조 파일은 수정하지 말고 구현을 별도 디렉터리에 작성한다. 시안의 코드 전체를 운영용으로 믿고 복사하지 않는다.

## 실제 추출한 토큰
| 변수 | 승인 파일 값 |
|---|---|
| `--bg` | `#111517` |
| `--sidebar` | `#151a1d` |
| `--surface` | `#191f23` |
| `--surface2` | `#20272c` |
| `--surface3` | `#252e33` |
| `--line` | `#30393e` |
| `--line-soft` | `#252e32` |
| `--text` | `#eef2f3` |
| `--body` | `#cbd3d7` |
| `--muted` | `#98a5ac` |
| `--faint` | `#89969e` |
| `--accent` | `#8ae0bf` |
| `--accent-ink` | `#153b2d` |
| `--accent-soft` | `#213a31` |
| `--blue` | `#a8c5ed` |
| `--blue-soft` | `#243343` |
| `--amber` | `#e7c085` |
| `--amber-soft` | `#393126` |
| `--red` | `#efaaaa` |
| `--red-soft` | `#3b292c` |
| `--radius` | `10px` |

글꼴 스택: `"Pretendard","Noto Sans CJK KR","Apple SD Gothic Neo","Malgun Gothic",sans-serif`. 이 패키지는 글꼴 파일을 포함하지 않는다.
실제 CSS 기준: 본문 14px/1.6, 제목 29px, 소제목 17px, 자소서 편집 15px/2.02. 예전 대화의 15px/16px 제안으로 바꾸지 않는다.

## 레이아웃
- 기본 데스크톱 사이드바 222px, 상단바 67px, 본문 최대 폭 1536px.
- 기본 본문 패딩 29px 32px 40px. 공고는 비교 표, 지원 준비는 콘텐츠 패널, 자소서는 편집기.
- 기본 자소서 그리드: 문항 목록 170px / 편집 영역 minmax(320px,1fr) / 근거 패널 250px, gap 18px.
- 15개 화면 ID와 구조는 PRODUCT 및 HTML PAGE_MAP 참조. 중복 임의 메뉴 추가 금지.
- 반응형은 원본의 1550/1180/930/720/560/390 관련 규칙과 실제 실행을 확인한다. 숫자만 복사하지 말고 좁은 화면의 하단 내비게이션·1열·표 스크롤을 검사한다.
- 대표 검증 폭: 1440, 1280, 768, 390px. 동일 기기/글꼴/데이터로 원본과 비교하고 글꼴 환경 차이는 명시한다.
- 초기 gate는 공통 셸+home/jobs/write이며 독립 QA 통과 전 다른 화면에 확장하지 않는다. 원본/실제/차이 이미지와 주요 영역 위치·크기를 남긴다. 기준 이미지는 승인 HTML에서만 생성하며 이미 생성된 기준 갱신에는 사용자 승인이 필요하다.
- 최초 검사 환경: Windows Microsoft Edge Chromium, DPR 1, 높이 900px, ko-KR/Asia-Seoul, 원본 2026-09-16 fixture, 빈 저장소·기본 탭. 같은 브라우저의 글꼴 차이를 최소화하고 픽셀 threshold 0.1/변경 비율 최대 0.1%, geometry는 정확 일치로 시작한다. 기준 실패를 숨기기 위해 허용값을 늘리지 않는다.

## 움직임 및 상호작용
- 버튼·내비 상태 전환 120ms. 페이지 진입 애니메이션 없음. 팝업은 원본처럼 즉시 표시.
- 페이지 등장, 카드 튕김, 지속 장식, 강제 타이핑 효과를 새로 넣지 않는다.
- prefers-reduced-motion을 존중. 포커스 표시, 키보드 이동, Escape 닫기, 포커스 복귀 유지.
- 민트는 주요 행동·선택, 앰버는 기한 주의, 파랑은 전형/정보, 빨강은 오류에 사용. 상태는 색뿐 아니라 글자로 표시.
- 저장 상태·작업 대기·실패·기한 미확인은 읽을 수 있는 문구로 표시. 실제 상태를 시연 스위치로 속이지 않는다.

## 시안과 실제 제품 사이의 의도적인 차이
1. 예시 날짜와 회사는 fixture로 격리. 실서비스에는 현재 날짜와 실제 DB 자료 사용.
2. 모의 로그인은 실제 세션 인증으로 대체. 비밀번호·Webhook은 localStorage에 저장하지 않음.
3. 모의 생성/배치는 실제 작업 상태로 대체. 연결 전에는 미연결/실패를 표시.
4. D-3 Slack 선택과 전형/회신 Slack 문구 제거. 모양은 유지하고 승인된 최종 정책만 반영.
5. 시안 검토 지도/피드백 버튼은 개발 전용 가능. 제품 핵심 행동은 제거하지 않음.
6. 원본의 고정 CSP(connect-src none 등)는 오프라인 시연용. 실제 앱 보안 정책은 별도로 설계하되 unsafe-inline을 관성적으로 복제하지 않음.

## 도구 사용
shadcn/ui·디자인 스킬은 재료와 검토 수단이며, 승인 파일보다 우선하지 않는다. Impeccable을 사용해도 목적은 ‘새 디자인 제안’이 아니라 ‘원본 일관성·오류·반응형 검토’다. 설치되어 있지 않으면 임의로 여러 디자인 스킬을 설치하며 구현을 지연하지 않는다.
레이아웃/색상/애니메이션 변경이 불가피하면 해당 화면, 이유, 최소 변경안, 비교 이미지를 사용자에게 제시해 승인받는다.
