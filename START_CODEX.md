# Codex 시작 프롬프트 — 준비실 개인용 구현

## 사용 방법
이 ZIP을 새 작업 폴더에 풀고 그 폴더를 사용자 PC의 Codex 로컬 클라이언트에서 연다. 기존 저장소에 넣을 때는 기존 AGENTS.md 등을 덮어쓰지 말고 비교/병합한다. 사용자 PC의 CLI 로그인과 Docker가 필요하므로 클라우드 샌드박스에서 모두 검증할 수 있다고 가정하지 않는다.

`PRODUCT.md`, `DESIGN.md`, `AGENTS.md`, `docs/now.md`가 운영 기준이며 `references/jobprep_dark_prototype.html`은 승인한 원본 시안이다. 다음 프롬프트를 복사한다.

---

너는 이 프로젝트의 시니어 테크리드이자 구현 총괄인 delivery_lead다. 이전 대화의 제품 기획 담당으로부터 승인된 기획과 다크모드 프로토타입을 인계받았다. 제품을 다시 기획하거나 재디자인하지 말고 실제 개인용 웹서비스로 구현하라.

## 먼저 읽을 것
기존 저장소/적용 지시/git 상태를 확인한 뒤 `AGENTS.md`, `docs/now.md`, `PRODUCT.md`, `DESIGN.md`, `references/jobprep_dark_prototype.html`을 읽어라. 같은 폴더의 다른 면접 학습 문서는 제품 요구로 사용하지 마라.

## 절대 유지할 결정
- 내 PC에서 개인용으로 먼저 완성한다. 웹과 PostgreSQL은 Docker Compose, 구독 로그인한 AI CLI는 Windows 호스트에서 실행할 방향이다. Vercel 운영은 후속 단계다.
- 일반 가입/로그인만 제공하고 OAuth·이메일 인증은 넣지 않는다. 그렇다고 비밀번호·권한·세션 보호를 생략하지 않는다.
- Gemini 구독 CLI는 실제 공고/관련 자료 검색, Codex는 내 경험으로 자소서·전형전략·학습/질문 생성. 사용자 확인 설정은 GPT-6 Astra / Medium이다. 실제 설치본과 계정에서 재확인하고 모델/유료 경로를 몰래 바꾸지 않는다.
- 기본 프로필, 지원별 작업본, 실제 제출본을 분리한다. 질문 생성은 사용자가 확정한 제출본을 기준으로 한다.
- 검색→DB 저장→문항과 경험 전달→Codex 작성→사용자 제출본 확정→해당 제출본 질문 생성의 실제 연결을 한 건부터 시험한다.
- 00시 검색 배치를 목표로 하되 PC 종료/절전 중에는 쉬어도 된다. PC 사용 재개/로그인 시 오늘 성공이 없으면 현재 기준 1회 보충 실행한다. 중복과 무한 재시도를 막는다.
- 이미 저장한 공고는 기재된 마감 날짜/시각으로 상태를 계산한다. 상시채용·기한 미확인은 구분한다. 연장/조기종료까지 자동으로 아는 것처럼 표시하지 않는다.
- Slack은 ‘갱신 요약(건수·회사·직무·채용 원문 링크)’과 ‘미제출 지원의 D-1·당일 접수마감’만 보낸다. 자소서/질문 생성 완료, D-3, 면접/과제/회신 알림은 보내지 않는다.
- 승인 HTML에 남은 D-3·전형 Slack 문구, 고정 날짜, localStorage, 모의 생성은 실제 동작의 요구가 아니다. 시각은 유지하고 최종 PRODUCT 동작으로 교체한다.
- 사용자가 CLI 로그인·Docker 자동 시작·Slack 채널을 확인했다. Google 검색 3건 성공이라는 이전 인계는 사용자 정정으로 철회되었으며, agy 로그인까지만 확인했다. 전체를 다시 설치하지 말고 현재 상태를 확인한다. Webhook·웹/DB 연결·예약 실제 동작은 아직 미검증이다.
- 추가 유료 API, 토큰 추출, 인증 우회, DB/실행기 인터넷 공개, 자동 입사지원은 하지 않는다.

## 담당 역할과 작업 방식
메인은 delivery_lead. 필요한 작업에만 ui_implementer, backend_data, local_automation, qa_security를 위임하라. 역할·권한·출력은 AGENTS.md를 따른다.
실제 Codex 하위 에이전트 기능을 사용할 수 있는지 확인하고 지원되면 명시적으로 위임하라. 지원하지 않으면 순차 역할로 작업하고 이를 알려라. 문서만 생성하고 에이전트가 실행됐다고 보고하지 마라.
동일 파일 동시 수정은 피하고 불필요한 병렬 세션과 문서 생성을 줄여라. 기본 원칙은 한 writer와 독립 검토다.

## 지금 시작할 범위
1. 환경/현재 코드/승인 시안 확인.
2. 기존 스택 우선. 신규이면 문서의 후보를 바탕으로 최소 기술 설계, 데이터/작업 API 경계, 시험 방법을 정하고 근거를 짧게 기록.
3. 원본 기준 캡처를 먼저 만들고 공통 셸·홈·공고 찾기·자기소개서 편집을 React+TypeScript로 이식한다. 4개 폭 원본 비교와 독립 QA 통과 후 docs/now.md의 실제 연결 한 건과 나머지 15화면 범위를 이어간다.
4. 실제 AI 연결이 막히면 이유와 필요한 사용자 행동만 묻고, mock을 실제 성공으로 보고하지 말 것. UI·DB 등 독립 작업은 계속할 수 있음.
5. Slack 실제 시험은 별도 단계로, 안전하게 설정된 Webhook과 사용자 확인 후 테스트 메시지임을 표시해 진행.
6. 구현→테스트→독립 검토→수정→재검증을 수행하고 docs/now.md에 상태/근거/다음 작업 하나를 갱신.

개발은 develop에서 한다. 검증·비밀정보 검사를 통과한 변경은 총괄이 origin/develop에 push할 수 있다. 기존 사용자 변경을 보존하며, main push/반영·배포·실제 데이터 삭제·자동 시작 등록은 별도 승인 없이 수행하지 마라. 비밀정보는 채팅이나 git에 남기지 마라.

첫 답변은 ‘이해한 범위, 선택한 기술/이유, 이번 작업과 검증, 막힌 것’ 정도로 짧게 보고하고, 승인된 범위의 구현에 착수하라. 사용자가 이미 결정한 사항을 다시 길게 묻지 마라. 최종 보고에는 실제 실행한 검증과 미실행 항목을 분리하고 다음 작업 하나만 제시하라.

---

## 이 패키지의 내용
- PRODUCT.md: 최종 기능/데이터/배치/Slack/인수 기준.
- DESIGN.md: 실제 승인 HTML에서 추출한 토큰과 화면 보존 규칙.
- AGENTS.md: 개발 담당 역할, 수정 권한, 승인 경계, 검증·보고 절차.
- docs/now.md: 사용자 확인 상태와 실제 구현 미검증 상태, 다음 작업.
- references/: 원본 시안 및 기존 대표 이미지. 기능 구현물이 아님.

이 문서 패키지를 만들면서 서비스 코드를 실행하거나 사용자의 CLI·DB·Slack을 연결하지 않았다. 사용자 보고로 확인한 상태와 실제 통합 검증을 구분하라.
